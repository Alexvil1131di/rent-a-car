enum VehicleStatus {
    AVAILABLE = 'AVAILABLE',
    RENTED = 'RENTED',
    MAINTENANCE = 'MAINTENANCE'
}

enum ReservationStatus {
    CONFIRMED = 'CONFIRMED',
    CANCELED = 'CANCELED',
    COMPLETED = 'COMPLETED'
}


enum UserRole {
    ADMIN = 'ADMIN',
    CLIENT = 'CLIENT'
}