export enum VehicleStatus {
    AVAILABLE = 'AVAILABLE',
    RENTED = 'RENTED',
    MAINTENANCE = 'MAINTENANCE'
}

export enum ReservationStatus {
    CONFIRMED = 'CONFIRMED',
    CANCELED = 'CANCELED',
    COMPLETED = 'COMPLETED'
}


export enum UserRole {
    ADMIN = 'ADMIN',
    CLIENT = 'CLIENT'
}